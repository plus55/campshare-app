# CampShare Handoff - 2026-05-24

## Current Integration Head

- Active branch: `codex/ui-remediation`
- Integration commit: `2ef090c` - merges Claude's latest UI checkpoint
  (`6920e71`) with `codex/audit-remediation` (`2c296fd`) and completes the
  light-theme UX remediation pass.
- Subsequent QA commit: `8591234` - mobile layout, trip status, and form/table
  accessibility corrections.
- The current branch additionally fixes the authenticated account dropdown:
  Base UI `DropdownMenuLabel` must remain inside `DropdownMenuGroup`, otherwise
  navigating to a signed-in route throws `MenuGroupContext is missing`.
- Development output is isolated in `.next-dev` so running `next build` no
  longer leaves `next dev` serving HTML for missing CSS assets from `.next`.
- Do not resume from `claude/current-work`; it is preserved as a pre-integration
  UI source branch.

## Behavior That Must Be Preserved

The integrated branch retains the audit-remediation correctness baseline:

- Turnstile enforcement on account/listing creation paths.
- Public pickup-location privacy: listing detail pages expose approximate
  regional coordinates, not exact host pickup coordinates.
- PaymentIntent ownership and booking amount/date/add-on validation.
- Booking transition locking, double-booking protection, cancellation safety,
  date-change restrictions, and payout idempotency.
- Expired-request authorization release via `expireBookingRequest`.
- Authenticated lifecycle cron handling, retryable Stripe webhooks, payout
  readiness checks, and hardened iCal processing.

Any future UI edits around bookings, trips, payouts, listings, auth, webhooks,
or maps must preserve this behavior and their API contracts.

## UX/UI Remediation Completed

- Kept Claude's Tailwind/shadcn visual direction, typography, palette, shared
  chrome, card treatment, and responsive approach.
- Removed the launch-time dark-mode path: no visible toggle, no active
  `ThemeProvider`, light-only toast/Stripe theming, and no `next-themes`
  dependency.
- Reconciled booking/trip/payout/detail screens against remediation behavior.
- Migrated rendered `cs-*` compatibility consumers to active shared/UI styles
  and removed obsolete compatibility CSS.
- Converted report and dispute modal flows to the shared accessible Dialog
  primitive.
- Added control labeling, live feedback, and keyboard-operable photo reorder
  controls across key transactional/admin forms.
- Kept Terms and Privacy visible as draft pages and set both routes to
  `noindex, nofollow`.

## Pre-Push Functional Pass (Working Tree)

This pass is implemented locally on `codex/ui-remediation` but must not be
pushed or handed back for further editing until the authenticated/Stripe QA
gate below is completed.

- Consolidated account-menu and dashboard sign-out through one client action
  with pending state, visible failure feedback, and immediate
  `router.replace("/login")` plus refresh on success.
- Added a shared booking-status policy and tests. `in_progress` is now active
  in host lists and messageable from the UI and API, while self-service
  cancellation remains limited to `requested` and `accepted`.
- Hardened notification loading/mark-read behavior so failed responses do not
  clear unread state or crash rendering.
- Hardened user-facing action failures across saved searches, wishlist,
  booking payment preparation/confirmation, payout dashboard links, profiles,
  listings, photos, add-ons, calendars/iCal, templates, reviews, and admin
  moderation surfaces. Added live error feedback and keyboard-operable
  calendar day actions where needed.
- Backed up local D1 data to
  `.wrangler/qa-backups/pre-functional-qa-20260524-211934`, then applied local
  migrations `011_ical_support.sql` and
  `014_booking_payment_integrity.sql`. Local schema verification confirmed
  `van_listing.icalFeedUrl`, `availability_block.icalUid`,
  `booking_transition_lock`, and `idx_payout_booking_unique`.
- Corrected the first-time profile completion experience after interactive QA
  showed that a successful create could appear inert: successful creation now
  shows confirmation and routes back to the dashboard, while existing-profile
  edits remain in place after save.
- Corrected QA defects surfaced during the first requested booking:
  link-based primary buttons were inheriting the global clay anchor colour,
  producing clay text on clay buttons; the default anchor styling now lives
  in Tailwind's base layer so button variants retain accessible contrast.
  Guest and host booking list actions now use compact outlined `Details`
  buttons instead of bare `View` links.
- Centralised user-facing NZD formatting so actual cents are retained in
  checkout, trips, booking details, payouts, listing prices, admin payment
  views, and transactional emails. The local QA booking stored and authorised
  at `NZ$3.41` was previously displayed as `$3`.
- Hardened booking messaging so notification-email failure cannot make a
  successfully persisted message appear to have failed. A save failure now
  produces specific visible feedback and a server diagnostic entry.
- Diagnosed the requested-booking message insert failure: migration `003`
  rebuilt `booking` through `booking_old`, and SQLite rewrote the pre-existing
  `booking_message.bookingId` foreign key to reference the dropped
  `booking_old` table. Added migration
  `015_booking_message_fk_repair.sql` to preserve message rows while restoring
  the foreign key to `booking(id)`.
- Backed up the active QA database again to
  `.wrangler/qa-backups/pre-migration-015-20260525-message-fk`, applied
  migration `015` locally only, and confirmed `booking_message.bookingId`
  now references `booking(id)` with no remaining `booking_old` schema
  references.
- Follow-up interactive QA confirmed booking messaging succeeds after
  migration `015` and an email notification is delivered. Host acceptance
  then succeeded: local D1 shows the booking in `accepted` state with
  `paidAt` set, and the Stripe `payment_intent.succeeded` and
  `charge.captured` webhook events processed.
- Fixed issues found during that host walkthrough: password sign-in now
  refreshes authenticated header chrome immediately; message sends create
  in-app notifications; notification destinations distinguish host and guest
  booking pages and date-change actions; received messages are marked read
  when their thread is opened; host booking rows surface unread-message
  badges; and accept/decline returns the host to the bookings list with
  visible success feedback.
- Corrected related review workflow routing found during the notification
  audit: review prompts now lead to the reviews workspace, and host review
  submission returns to `/dashboard/reviews` rather than the guest-only trip
  detail route.

## Verification Completed

- `npm run typecheck` passed.
- `npm run test:run` passed: 38 tests include the booking-status policy, NZD
  cents-display, and notification-destination regression coverage added in
  the pre-push pass.
- `npm run build` passed.
- Re-ran all three checks after correcting the signed-in `UserMenu` Base UI
  group context failure.
- A temporary production server was validated for public routes:
  `/`, `/vans`, `/login`, `/signup`, `/apply`, `/terms`, and `/privacy`.
- Confirmed `/terms` and `/privacy` emit `noindex`.
- Confirmed the homepage does not expose theme-toggle/provider output.
- Confirmed no active TSX/CSS source consumer remains for legacy `cs-*`
  presentation classes and no runtime `next-themes` import remains.
- Ran the app from the supported auth origin `http://localhost:3000` only;
  stopped the unsupported port `3010` preview.
- Confirmed HTTP `200` responses for `/`, `/vans`, a local listing detail,
  its linked host profile, `/hire/auckland`, `/login`, `/signup`, `/apply`,
  `/terms`, and `/privacy`; confirmed the development CSS asset returns `200`.
- Confirmed unauthenticated `/dashboard` redirects to `/login`.
- Interactive QA completed on 2026-05-25 at `http://localhost:3000`:
  account-menu sign-out passed; host Stripe Connect onboarding completed for
  the approved local host account; guest signup/email verification and Stripe
  Identity verification completed for the approved local guest account.
- Configured local Stripe test-only secrets and official Stripe CLI webhook
  forwarding. Confirmed a forwarded `payment_intent.created` test event was
  stored in local D1 with status `processed`; Connect onboarding and Identity
  webhook events generated during interactive QA were also processed.
- A non-instant booking was submitted in Stripe test mode. Local D1 stores
  `totalCents = 341` and its PaymentIntent; the guest saw `Pending Response`.
  A subsequent guest
  message attempt returned `Could not save message. Please try again.` without
  inserting a `booking_message`, leading to the migration `015` fix above.
  After applying that local migration, messaging and email passed and the host
  accepted the booking successfully.

Build warnings remain for Wrangler experimental configuration and an existing
Sentry/OpenTelemetry dynamic dependency trace; these do not fail the build.

## Remaining Release Work

- Conduct authenticated guest, host, and admin visual walkthroughs with
  realistic local-only data. Sign-out, host Stripe onboarding, and guest KYC
  are complete; requested messaging, payment capture on host acceptance, and
  acceptance webhook handling are complete. Retest in-app message
  notifications/header refresh and accepted-trip messaging, then verify
  cancellation restrictions, listings/calendar/iCal, payouts, reports,
  disputes, and moderation remain to be verified.
- Run keyboard-only and automated accessibility checks in an interactive
  browser for navigation, booking, listing-management, messaging, and trust
  flows.
- Decide security-deposit/damage-claim and cancellation fee/GST policy before
  further payment-policy work.
- Complete lawyer approval for legal content and remove noindex only after
  approval.
- Before deploying this branch, verify and manually apply remote D1 migrations
  `011_ical_support.sql` through `015_booking_message_fk_repair.sql` in order;
  this repository does not use an automatic migration runner.
- Verify production Worker secrets for auth, email, photo uploads, Turnstile,
  Stripe, webhooks, and cron handling: `BETTER_AUTH_SECRET`,
  `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `RESEND_API_KEY`, `EMAIL_FROM`,
  `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`,
  `TURNSTILE_SECRET_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, and
  `CRON_SECRET`.
- Complete Stripe live-mode, device QA, and controlled real-payment
  verification before launch.

## Host Insurance + Deposit-Claim Work (2026-05-25)

Added host-carries-cover insurance verification and wired real deposit-claim
execution. Decisions: host-carries-cover (no platform underwriting),
founder-owned launch supply, GST stays on the service fee only, flat $500
deposit. Written policy lives at the `/policies` route (noindex draft).

- Migration `016_host_insurance.sql` adds insurance columns to `host_profile`
  and a `deposit_claim` ledger. **Apply locally then remotely** (no auto-runner).
- Insurance gate (`src/lib/insurance.ts` → `isInsuranceCurrent`) blocks listing
  submit, admin approve, and booking acceptance unless cover is verified and
  in-date. The daily `booking-lifecycle` cron now also expires lapsed policies
  and pauses affected listings (Pass 5).
- Host docs upload to a **new PRIVATE bucket `campshare-host-docs`** (bound as
  `HOST_DOCS` in wrangler.jsonc). **Create the bucket and keep it non-public**;
  admin views docs via short-lived presigned GET. R2 SigV4 code is now shared in
  `src/lib/r2-presign.ts` (photos/sign refactored to use it — behaviour same).
- Admin dispute resolution now EXECUTES deposit claims: awarding the deposit to
  the host charges the guest's saved payment method off-session (≤ $500) and
  transfers to the host (`src/app/api/admin/disputes/[id]/route.ts`). Tracked in
  `deposit_claim` for idempotency; the hold-release vs 7-day claim-window gap is
  closed this way.
- Owner actions still outstanding: insurance-broker confirmation of acceptable
  proof wording, accountant confirmation of GST model, founder uploads +
  self-approves own commercial policy, and live-mode testing of the off-session
  deposit charge.

## Claude Resume Instructions

Start from `codex/ui-remediation`, not `claude/current-work`. Read this file
and `docs/WORKSTREAMS.md` first. Treat `2ef090c` as the behavior and UI
baseline, plus preserve `8591234` and the authenticated `UserMenu` group
context correction. Do not reintroduce dark mode, exact public pickup
coordinates, or any pre-remediation booking/payment/payout implementation.
Focus next on authenticated visual QA, responsive/accessibility defects found
through that QA, and launch-readiness items explicitly approved by the owner.
