# CampShare Handoff - 2026-05-24

This is the pause point before further integration or product-policy changes.

## Saved Branch Heads

- `main`: `9d61890` - shared workstreams record only; neither application
  workstream has been merged into main.
- `codex/audit-remediation`: `67b1150` - pushed and verified launch-critical
  security and payment remediation.
- `claude/current-work`: `caf56f3` - pushed WIP checkpoint preserving Claude's
  interrupted multistage Tailwind/shadcn UI migration.

## Codex Checkpoint

The Codex branch contains:

- `a51581c` - Turnstile enforcement, XSS protection, public location privacy,
  and hardened iCal imports.
- `51245ec` - booking transition locking, double-booking prevention,
  cancellation failure handling, restricted date changes, and payout
  idempotency.
- `b866e08` - Stripe PaymentIntent ownership, amount, dates, guest count, and
  add-on authorization validation.
- `67b1150` - expired-request authorization release, authenticated lifecycle
  cron execution, recoverable webhook retries, and consistent Stripe payout
  readiness.

Verification completed on this branch before pause:

- `npm run typecheck`
- `npm run test:run` - 29 tests passing
- `npm run build`

The successful build still reports deployment follow-ups: Better Auth requires
the production secret, Wrangler reports experimental `unsafe` configuration,
and Sentry/OpenTelemetry emits an existing bundling warning.

## Claude UI Checkpoint

Claude's branch contains the UI migration foundation and partial page
migration:

- `d54b3d7` through `c398d72` - Tailwind/shadcn foundation, site chrome, and
  homepage work.
- `417b85a`, `16ca2f5`, `364bf4f` - Stage 5 page migration work.
- `caf56f3` - WIP preservation commit for interrupted edits in message
  templates and trip review pages.

The WIP branch has been preserved rather than completed or integrated. It
should be tested after reconciling it with the Codex correctness changes.

## Known Integration Overlap

Claude's UI migration and Codex's payment corrections both modify:

- `src/app/trips/page.tsx`
- `src/app/dashboard/bookings/page.tsx`
- `src/app/dashboard/payouts/page.tsx`

Codex also modifies related detail paths:

- `src/app/trips/[id]/page.tsx`
- `src/app/dashboard/bookings/[id]/page.tsx`

When resolving conflicts, preserve the centralized expired-booking
authorization release flow and Stripe payout-readiness checks from Codex,
while retaining Claude's intended visual migration.

## Resume Order

1. Review and merge the Codex remediation commits into `main`.
2. Rebase `claude/current-work` onto the updated `main`, resolve the overlap
   files deliberately, then complete and test the UI migration.
3. Decide the security-deposit/damage-claim model and cancellation treatment
   of service fees and GST before implementing further payment-policy changes.
4. Complete production secret, Stripe, migration, device QA, and controlled
   live-payment verification before launch.
